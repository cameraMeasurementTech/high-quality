# Vendored dependencies (auto-populated)

Created by `./run/00_bootstrap_assets.sh`:

| Path | Purpose |
|------|---------|
| `shiny-guide/` | King pipeline + vLLM spawn (`pipeline_service/`) |
| `pipeline_prompts/scene_coder/` | Snapshot of coder prompts for DPO dataset packing |

Do not edit manually — re-run bootstrap after upgrading shiny-guide.
