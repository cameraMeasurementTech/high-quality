# Training pipeline deps for the image->Three.js coder VLM (SFT + DPO + GRPO).
# Pin to known-good ranges; bump deliberately and re-test the audit.
#
# Install on the GPU training box:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#
# NOTE: vllm + torch are heavy and CUDA-specific. If pip resolves a CPU torch,
# install the matching CUDA wheel first (see COOKBOOK.md step 1).

# Core training stack
torch>=2.4.0
transformers>=4.51.0
trl>=0.15.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0
bitsandbytes>=0.44.0
deepspeed>=0.15.0

# Rollouts / serving for GRPO
vllm>=0.6.3

# VLM image + reward helpers
pillow>=10.0.0
qwen-vl-utils>=0.0.8
httpx>=0.27.0
openai>=1.40.0
numpy>=1.26.0
tqdm>=4.66.0
pyyaml>=6.0

# DINOv3 embeddings for the reward (gated: needs HF_TOKEN + Meta license)
# transformers already provides the model classes; timm helps some checkpoints.
timm>=1.0.0

# Optional experiment tracking
wandb>=0.17.0
