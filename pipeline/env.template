# Copy to training/.env — pipeline section uses same file as training.
# See ../.env.template for full list.

OPENROUTER_API_KEY=sk-or-v1-...
HF_TOKEN=hf_...

# Local coder weights (optional — else pulls from HuggingFace on first boot)
# MODEL_PATH=/path/to/Qwen-3.6-27B-AstroWolf

# HF_HOME defaults to training/data/hf_cache
